namespace Inferior.UI;

/// <summary>Horizontal text/content alignment within a control's bounds.</summary>
public enum HAlign { Left, Centre, Right }

/// <summary>Vertical text/content alignment within a control's bounds.</summary>
public enum VAlign { Top, Middle, Bottom }
